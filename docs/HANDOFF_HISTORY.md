# 二人目の作業履歴（参考資料）

この文書は過去の記録です。未完了・未公開・作品数などは記録当時の状態を含みます。最新の判断には ../HANDOFF.md を使用してください。

# 作業引き継ぎ

## 二人目の開始時点（2026-09-07）

- ユーザーと今回の担当者は、このプロジェクトに初めて触れる。
- 今回は二人目の作業者。三人目へ経緯・変更・検証結果・残作業を渡すところまでを担当する。
- リポジトリ: https://github.com/kura-masa/portfolio-shell
- 作業場所: `D:\開発\portfolio`
- 開始コミット: `a5149aaaa2bbf5a84c67fc5c923fdf4338810874`（`master`）
- 空の作業フォルダーに clone。開始時の作業ツリーはクリーン。
- README、全3コミットの履歴、index.html、同期スクリプト、preview.htmlとの差分を確認。
- GitHubの未完了Issue・PRは確認時点で0件。AGENTS.md、package.json、CI設定はリポジトリ内にない。

## 構成と継承する決定

- ビルド不要の静的サイト。`index.html` にCSS、作品データ（PRODUCTS）、表示・操作用JavaScriptが入っている。
- 3人の作品を紹介するサイトで、現在は10作品。掲載作品の最終確定はまだ。
- 美咲ゴシックを使用。既存の指針は文字サイズを8の倍数にすること。
- プレビューは動画→静止画→SVGの代替表示。ゲーム本体は作品を開いたときに読み込む。
- 2026-09-05の決定: STG・ブロックくずしの制作者は表示しない。ゲームのソースリンクはmedaka333のリポジトリ。
- タワーバトルは既存の外部公開版へリンクする。自前ビルドには画像欠落の既知情報がある。
- ヘッダーは「ポートフォリオ（仮）」のまま公開する方針。フォントの扱いもREADMEに前任時点の決定がある。

## ソースとファイルで確認できた残課題

1. `games/` は `.gitkeep` のみ。STG・ブロックくずしのローカルリンク先は存在しない。元リポジトリのWeb成果物の準備が必要。
2. READMEは映像が2本と記載しているが、実際は9本ありindex.htmlから参照されている。タワーバトルのみ動画指定なし。再生品質は未検証。
3. READMEのゲーム配置節にはタワーバトルの配置が未決定という古い説明が残る。末尾の決定事項と矛盾する。
4. `sync-games.sh` は今もanimalVSの自前ビルドを同期しようとする。bashとrsyncを使用し、現在のWindows環境での実行は未検証。
5. `preview.html` は一部動画をdata URLで埋め込んだ別版。ゲームの制作者表記・ソースリンク・タワーバトルのリンクが古い。用途を確認して扱いを決める。
6. ゲームから戻る動線がないという前任の記録がある。今回の担当者はまだ再現確認していない。

## 検証範囲と次の入口

今回の初回確認はコード・Git履歴・ファイル構成の読み取りまで。ブラウザ表示、動画再生、キーボード・タッチ操作、外部作品の稼働、ゲーム起動、配信時の圧縮は未検証。

次はローカルで表示・操作を確認し、現状の画面をユーザーと共有する。掲載内容や改修の優先順位は、その確認を踏まえて決める。

この時点での追加はこのメモのみ。実装変更、公開、commit、pushは行っていない。

## 三人目へ渡す前に追記すること

- ユーザーと決めた内容と、その理由。
- 実際に変更したファイル・コミット。
- 実施した検証、その結果、未検証の範囲。
- 起動・公開手順、必要な依存リポジトリや成果物。
- 残課題と次に着手する具体的な箇所。

## ローカル動作確認と初回修正（2026-09-07）

ユーザーの「進めよう」を受け、上記の初回調査から動作確認へ進んだ。以下が現在の状態。

### 起動

PowerShellで作業フォルダーから `python -m http.server 4173 --bind 127.0.0.1`。
表示先は http://127.0.0.1:4173/ 。この作業ではサーバーを起動したままにしている。

### 修正したこと

- `index.html`: Tabでフォーカスした作品と表示中の作品が食い違い、Enterで別作品を開く不具合を修正。修正前はタワーバトルにフォーカスしてもSTGへ遷移して404になった。
- 矢印キー移動でもフォーカスを選択行へ移す。詳細リンクのEnterは、そのリンク本来の遷移を使う。ポインターによるfocusinでは作品を先に選択せず、既存のタッチ操作への影響を避ける。
- 動画・静止画を `object-fit: contain` に変更。Webアプリ動画は約2.34:1、枠は16:9なので、従来のcoverでは左右が切れていた。
- `README.md`: 動画9本・タワーバトルは外部版利用という現在の状態を反映。animalVSのビルドは通常不要だが同期スクリプトに旧処理が残ることを明記。
- `preview.html` は用途が未確定のため今回の変更を反映していない。確認は `index.html` を正として行う。

### 実施した検証

- Codex内ブラウザで1280×720の表示と390×844の1列表示を目視確認。狭い画面で横方向のはみ出しなし。詳細は一覧の下にある。
- 全10作品の矢印キーによる切り替えを確認。9本の動画がreadyState=4になり、映像寸法を取得できることを確認。各動画の全編・内容の正確性までは確認していない。
- 修正後、Tabでタワーバトルへ移動すると詳細も一致し、Enterで外部版のタイトル画面へ到達。
- 「ソースを見る」にフォーカスしてEnterを押すとmedaka333/famicongameへ到達。
- 矢印移動後のフォーカスと選択作品の一致、保険営業支援アプリ動画の左右を含む全体表示を確認。
- `git diff --check` は問題なし。

### 残作業

- STG・ブロックくずしの成果物を用意し、シェルからゲームを起動する。現在は本体未配置で、STGの404はブラウザでも確認済み。
- タッチ実機の2段階タップは未検証（幅変更のみではタッチ端末の検証にならない）。
- タワーバトルのプレイ本編、他の外部作品の稼働、全動画の内容、公開環境の圧縮配信は未検証。
- スマートフォンで詳細までのスクロールが長い点、長い作品名の折り返しは今後ユーザーと改善方針を決める。
- `preview.html` の用途整理、Windows向けゲーム準備手順、掲載内容の確定、公開先の決定。

今回の変更は未コミット・未push・未公開。

## PC・スマホ共通基盤への改修（2026-09-07）

### 最新のユーザー方針

STG・ブロックくずしは一人目のPCで正常動作しており、一人目がホスティングして公開する。
**二人目のPCにゲーム本体を配置する必要はない。** 上記の本体導入を次作業とした記録は撤回。
公開URLを受領してPRODUCTSのリンクを差し替えることだけが残る。

ユーザーからスマホ幅が非常に見にくいとの指摘を受け、PC・スマホで使える共通基盤へ改修した。

### 最終的な構成

- `index.html` が唯一の表示実装。データは引き続きPRODUCTSの10件。
- 761px以上は左の一覧・右の詳細。760px以下は端末標準の作品選択メニュー・詳細。
- 全幅で前後の作品へ移動するボタンと件数表示を使用。
- クリック／タップは選択のみ。リンクボタンから外部サイトを開く。ホバーや2回目タップによる自動遷移を廃止。
- 一覧は選択行だけTab停止。矢印・Home・Endで移動。前後ボタンや詳細リンクは標準のキーボード操作。
- ブランドとカーソルは美咲ゴシック、作品名・本文・操作部は通常ゴシック。本文は空行で段落分けし、端末幅で折り返す。
- 動画枠は共通16:9で、素材は全体表示。選択中だけ再生し、他はpause。
- `preview.html` の古い表示コードと埋め込み映像を、本体への転送とリンクに置き換えた。旧版はGit履歴から参照可能。
- READMEの表示・操作・書体と担当範囲を更新。

### 検証

- ブラウザで320 / 390 / 760 / 768 / 1024 / 1440pxの6幅×10作品を前後ボタンで巡回し、横のはみ出しなし、選択メニューと一覧の選択一致、プレビュー幅を確認。
- その後CSS重複を整理し、PCの長い一覧行が縮んで副題がはみ出す問題をflex-shrink:0で修正。最終CSSでも6幅を再確認し、横のはみ出しと行内のはみ出しがないことを確認。
- 390px幅で選択メニューからLOCAを選び、長い名前・説明・映像・リンクを目視確認。
- PCで一覧クリックはページ遷移せず選択され、矢印移動でフォーカスと選択が一致。前へボタンの動作と非表示動画の停止を確認。
- preview.htmlを開くとindex.htmlへ到達することをブラウザ確認。
- スマホ実機のSafari/ChromeとOS標準選択UIは未検証。ブラウザの幅変更と選択操作による検証まで。

### 引き続き残るもの

- 一人目から公開URLを受領して2ゲームのリンク差し替え。
- 掲載作品・説明文・制作クレジットの最終確定、タワーバトルの映像。
- 公開環境・スマホ実機での確認。

変更はローカルのみ。未コミット・未push・未公開。

## 指定4プロジェクトの掲載（2026-09-07）

- ユーザー指定の4名称は、GitHubリポジトリ名ではなくローカル台帳のprojectIdだった。`D:/開発/遊び開発/project-manager/data/projects.json`と各プロジェクト資料から照合した。台帳自体には変更していない。
- `human-stack-battle-20260715`: 既存の「ともだちたわーばとる」と同じ公開URL。重複追加せず、既存項目のidを台帳IDに合わせた。前任決定のソースリンクとクレジットは維持。
- `gijiro-winwinreco`: GIJIRO / WinWinRecoとして追加。公開URL `https://winwinreco-227823039350.asia-northeast1.run.app/` と公開GitHub `ninnzinn3bonn-creator/winwinreco` へリンク。ブラウザで公開の歓迎画面を確認。会議の作成や送信はしていない。
- `yururi-landing-page`: 城崎 柳通り ゆるりとして追加。公開URL `https://yururi-landing-page.pages.dev/` をブラウザで確認。リポジトリはprivateのためソースボタンは付けていない。
- `local-project-register`: 開発プロジェクト台帳として追加。PCで動かすローカルアプリで、公開Web版は未確認。訪問者向けにlocalhostへリンクせず、公開GitHub `https://github.com/ninnzinn3bonn-creator/pj-` へ案内。
- 合計13作品。新規3件の制作者表記は断定せず未指定。映像がない4件は既存SVGの代替プレビューを表示。
- 指定4件のPC・390px幅の選択、件数、リンク、横にはみ出さないことを確認。READMEの件数と未収録対象を更新。
- デモ動画について、現在のブラウザ操作APIに録画保存機能がなく、サイト部分だけを安定して録画する手順を確認できなかった。ffmpegは導入済みだが、今回動画ファイルは作成していない。ユーザーへ録画を依頼する方針。動画を受領したらdemosへ配置しPRODUCTSのdemoを指定する。
- 未コミット・未push・未公開。

## 提供動画4本の編集・組み込み（2026-09-07）

ユーザーがルートへ置いたGIJIRO.mp4 / PJ.mp4 / たわーばとる.mp4 / ゆるりLP.mp4を内容確認して編集。
4件のPRODUCTSにdemoとposterを追加し、全13作品が動画付きになった。
採用区間・加工条件・出力ファイルはdemos/README.mdを参照。

- 等速で17〜21.5秒に整理。待ち時間・撮影画面・重複操作を削り、ブラウザ欄・黒帯を除去。
- 音声なしH.264 MP4、短いフェード、代替JPEGを生成。合計約2.8MB。
- 原本は変更せずローカルに保持。.gitignoreに原本4本と.work-video/を追加し、配信版だけGit管理する。
- 編集前は各3秒間隔の画像で全体構成を確認。編集後は静止画とFFmpeg全デコード、ffprobeで尺・寸法・音声なしを確認。
- PCと390px幅で4本ともreadyState=4、paused=false、横はみ出しなし。PCでミュート・ループ指定も確認。
- 未コミット・未push・未公開。前の「動画未収録」「ユーザーへ録画依頼」は今回完了。

## 全リンクの接続確認（2026-09-07）

ユーザーの質問に対し、index.html内の全リンク先を重複除去して22 URLを未認証HTTP GETで確認。
16件はHTTP 200。STGとブロックくずしのローカルリンクは既知の公開URL待ちで404。
外部アプリ10件はすべてHTTP 200（旅館ダッシュボードは/loginへ移動）。アプリ内部の各機能までは検証していない。

次のGitHubソースリンク4件は未認証で404。非公開・削除・移転のどれかは断定できない。
- https://github.com/kura-masa/loca-the-junpu-ai-chatbot
- https://github.com/kura-masa/junpu-dashboard
- https://github.com/kura-masa/bird_hoken_app
- https://github.com/kura-masa/IdeasAndKnowledgesArticle

ユーザーへの報告のみで、この確認ではリンクを変更していない。公開用のソースボタンは今後非表示または正しい公開URLへ変更する必要がある。

## Gitへの保存対象（2026-09-08）

ユーザーの指示により、PC・スマホ共通表示、作品追加、編集済み動画4本・代替画像、READMEと本引き継ぎ資料を一式でコミットし、origin/masterへプッシュする。
前節までの「未コミット・未push」は各作業時点の記録。最新の保存状態はGit履歴とorigin/masterとの比較で確認すること。

原本4本と.work-video/はローカルに保持しGit対象外。未解決事項はSTG・ブロックくずしの公開URL受領待ちと、GitHubソースリンク4件の404。今回の保存では変更しない。
保存前にoriginをfetchし、既存HEADとorigin/masterの差分なし、JavaScript構文検査とgit diff --checkの成功を確認した。
